#include<iostream>

using namespace std;

int main() {
    int n;
    cout<<"Enter N : ";
    cin>>n;

    // (condition) ? if true : else false ;
    
    (n % 2 == 0) ? cout<<"Even" : cout<<"Odd";
     
}